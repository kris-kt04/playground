services:
  devpod:
    build:
      context: .
      dockerfile: dev-env.dockerfile
    volumes:
      - ..:/workspace
    sysctls:
      - net.ipv6.conf.all.disable_ipv6=1
    command: ["sleep", "infinity"]
